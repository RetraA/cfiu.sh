# cfiu.sh---Cloudflare-IP-Updater-DDNS-
CFIU is a fully automated Bash script that keeps Cloudflare DNS records up to date with the system’s current public IPv4 and IPv6 addresses. It is designed for dynamic IP environments and supports multiple domains, multiple zones, and multiple record types in a single configuration file.
